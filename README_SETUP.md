Repository setup

1. Create virtual environment and activate it:

```bash
python3 -m venv .venv
source .venv/bin/activate
```

2. Install dependencies:

```bash
pip install -r requirements.txt
```

3. Run tests:

```bash
pytest -q
```

4. Open `example.ipynb` in Jupyter Lab / Notebook to run the interactive demo.
